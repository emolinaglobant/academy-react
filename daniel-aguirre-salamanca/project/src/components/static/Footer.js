import styles from "./Footer.module.css";

const Footer = () => {
  return (
  <footer className={styles.footer}>
      &copy;Desarrollado por Daniel Aguirre (Donvito)
  </footer>
    );
};

export default Footer;
