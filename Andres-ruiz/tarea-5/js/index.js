const slider = {primero:[1, 2, 3, 4], segundo:[5, 6, 7, 8], tercero:[9, 10, 11,12 ], cuarto:[13, 14, 15, null]
}

function recorrer(n) {
    for(let i=0; i<= n.length; i++){
        const puzzel = document.getElementById('puzzel');
        const numero = document.createElement('button');
        puzzel.appendChild(numero);
     }
}
recorrer(slider.primero);


